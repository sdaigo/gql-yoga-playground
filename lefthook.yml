pre-commit:
  parallel: true
  jobs:
    - name: biome check
      glob: "*.{js,ts,jsx,tsx}"
      run: bunx @biomejs/biome check {staged_files}

    - name: biome format
      glob: "*.{js,ts,jsx,tsx}"
      run: bunx @biomejs/biome format --write {staged_files}
